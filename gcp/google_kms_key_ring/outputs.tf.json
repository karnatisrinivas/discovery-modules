{
    "output": {
        "id": {
            "description": "An identifier for the resource with format projects/{{project}}/locations/{{location}}/keyRings/{{name}}",
            "value": "${google_kms_key_ring.this.id}"
        }
    }
}
