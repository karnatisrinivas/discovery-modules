{
    "output": {
        "etag": {
            "description": "Etag for tracking changes to the IAM policy binding",
            "value": "${google_spanner_instance_iam_member.this.etag}"
        },
        "id": {
            "description": "Unique ID for the IAM member binding on the Spanner instance",
            "value": "${google_spanner_instance_iam_member.this.id}"
        }
    }
}
