{
    "output": {
        "etag": {
            "description": "ETag for tracking changes to IAM policy bindings on the subscription",
            "value": "${google_pubsub_subscription_iam_member.this.etag}"
        },
        "id": {
            "description": "Unique identifier for managing IAM member associations with the subscription",
            "value": "${google_pubsub_subscription_iam_member.this.id}"
        }
    }
}
