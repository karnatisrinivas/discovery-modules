version: 1.0

representation:
  side_panel:
    label: Pubsub Subscription Iam Member
  node:
    label:
      static: Pubsub Subscription Iam Member
      template: ${subscription}
    display_type: default

discovery:
  type: google_pubsub_subscription_iam_member
  dnd_supported: false
  references:
    subscription:
      from: google_pubsub_subscription
      output: id
