{
  "variable": {
    "location": [
      {
        "description": "Specifies the region or zone where the Kubernetes cluster will be created; must match available locations in Google Cloud.",
        "nullable": true,
        "type": "string"
      }
    ],
    "name": [
      {
        "description": "Specifies the name of the Kubernetes cluster, which must be unique within the project and region.",
        "nullable": false,
        "type": "string"
      }
    ]
  }
}
