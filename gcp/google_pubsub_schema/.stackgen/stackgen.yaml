version: 1.0

representation:
  side_panel:
    label: Pubsub Schema
  node:
    label:
      static: Pubsub Schema
      template: ${name}
    display_type: default

discovery:
  type: google_pubsub_schema
  dnd_supported: false
