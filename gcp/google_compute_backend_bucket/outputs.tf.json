{
    "output": {
        "creation_timestamp": {
            "description": "Timestamp indicating when the backend bucket was created",
            "value": "${google_compute_backend_bucket.this.creation_timestamp}"
        },
        "id": {
            "description": "Unique identifier for referencing the backend bucket",
            "value": "${google_compute_backend_bucket.this.id}"
        },
        "self_link": {
            "description": "URL for accessing the backend bucket resource",
            "value": "${google_compute_backend_bucket.this.self_link}"
        }
    }
}
