{
    "output": {
        "id": {
            "description": "Unique identifier for linking the provider in other configurations",
            "value": "${google_iam_workload_identity_pool_provider.this.id}"
        },
        "name": {
            "description": "Human-readable name for identifying the provider in the workload identity pool",
            "value": "${google_iam_workload_identity_pool_provider.this.name}"
        },
        "state": {
            "description": "Current operational status of the workload identity pool provider",
            "value": "${google_iam_workload_identity_pool_provider.this.state}"
        }
    }
}
