version: 1.0

representation:
  side_panel:
    label: Compute Health Check
  node:
    label:
      static: Compute Health Check
      template: ${name}
    display_type: default

discovery:
  type: google_compute_health_check
  dnd_supported: false
