version: 1.0

representation:
  side_panel:
    label: Compute Instance Settings
  node:
    label:
      static: Compute Instance Settings
      template: ${zone}
    display_type: default

discovery:
  type: google_compute_instance_settings
  dnd_supported: false
