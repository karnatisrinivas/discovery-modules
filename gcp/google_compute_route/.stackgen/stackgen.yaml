version: 1.0

representation:
  side_panel:
    label: Compute Route
  node:
    label:
      static: Compute Route
      template: ${name}
    display_type: default

discovery:
  type: google_compute_route
  dnd_supported: false
  references:
    network:
      from: google_compute_network
      output: self_link
