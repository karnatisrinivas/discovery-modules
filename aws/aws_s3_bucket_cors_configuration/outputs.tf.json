{
    "output": {
        "id": {
            "description": "bucket and expected_bucket_owner separated by a comma (,) if the latter is provided.",
            "value": "${aws_s3_bucket_cors_configuration.this.id}"
        }
    }
}
