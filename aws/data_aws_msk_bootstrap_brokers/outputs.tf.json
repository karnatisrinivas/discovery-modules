{
    "output": {
        "bootstrap_brokers": {
            "description": "The endpoints to connect to the AWS MSK cluster.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers}"
        },
        "bootstrap_brokers_public_sasl_iam": {
            "description": "Public endpoints for connecting to the AWS MSK cluster using SASL IAM authentication.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_public_sasl_iam}"
        },
        "bootstrap_brokers_public_sasl_scram": {
            "description": "Public endpoints for connecting to the AWS MSK cluster using SASL SCRAM authentication.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_public_sasl_scram}"
        },
        "bootstrap_brokers_public_tls": {
            "description": "Public endpoints for connecting to the AWS MSK cluster using TLS encryption.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_public_tls}"
        },
        "bootstrap_brokers_sasl_iam": {
            "description": "Endpoints for connecting to the AWS MSK cluster using SASL IAM authentication.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_sasl_iam}"
        },
        "bootstrap_brokers_sasl_scram": {
            "description": "Endpoints for connecting to the AWS MSK cluster using SASL SCRAM authentication.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_sasl_scram}"
        },
        "bootstrap_brokers_tls": {
            "description": "Endpoints for connecting to the AWS MSK cluster using TLS encryption.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_tls}"
        },
        "bootstrap_brokers_vpc_connectivity_sasl_iam": {
            "description": "VPC endpoints for connecting to the AWS MSK cluster using SASL IAM authentication.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_vpc_connectivity_sasl_iam}"
        },
        "bootstrap_brokers_vpc_connectivity_sasl_scram": {
            "description": "VPC endpoints for connecting to the AWS MSK cluster using SASL SCRAM authentication.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_vpc_connectivity_sasl_scram}"
        },
        "bootstrap_brokers_vpc_connectivity_tls": {
            "description": "VPC endpoints for connecting to the AWS MSK cluster using TLS encryption.",
            "value": "${data.aws_msk_bootstrap_brokers.this.bootstrap_brokers_vpc_connectivity_tls}"
        },
        "id": {
            "description": "The identifier of the AWS MSK bootstrap brokers data source.",
            "value": "${data.aws_msk_bootstrap_brokers.this.id}"
        }
    }
}
