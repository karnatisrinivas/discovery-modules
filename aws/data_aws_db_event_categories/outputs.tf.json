{
    "output": {
        "event_categories": {
            "description": "A list of available event categories for the specified AWS RDS service.",
            "value": "${data.aws_db_event_categories.this.event_categories}"
        },
        "id": {
            "description": "The unique identifier for the AWS RDS event categories data source.",
            "value": "${data.aws_db_event_categories.this.id}"
        }
    }
}
