{
    "output": {
        "id": {
            "description": "The unique identifier for the VPC Lattice resource policy.",
            "value": "${data.aws_vpclattice_resource_policy.this.id}"
        },
        "policy": {
            "description": "The actual policy document of the VPC Lattice resource in JSON format.",
            "value": "${data.aws_vpclattice_resource_policy.this.policy}"
        }
    }
}
