version: 1.0

representation:
  side_panel:
    label: AWS IAM OpenID Connect Provider
  node:
    label:
      static: AWS IAM OpenID Connect Provider
      template: ${url}
    display_type: default

discovery:
  type: aws_iam_openid_connect_provider
  dnd_supported: false
  data_source: data_aws_iam_openid_connect_provider
