{
    "output": {
        "arn": {
            "description": "The value of the name output",
            "value": "${aws_iam_user.this.arn}"
        },
        "name": {
            "description": "The value of the name output",
            "value": "${aws_iam_user.this.name}"
        }
    }
}
