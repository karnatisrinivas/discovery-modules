version: 1.0

representation:
  side_panel:
    label: Kms Key Policy
  node:
    label:
      static: Kms Key Policy
      template: ${key_id}
    display_type: default

discovery:
  type: aws_kms_key_policy
  dnd_supported: false
  references:
    key_id:
      from: aws_kms_key
      output: id
