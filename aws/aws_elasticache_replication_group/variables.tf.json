{
    "variable": {
        "snapshot_retention_limit" : [{
            "default": 0,
            "description": "The number of days for which ElastiCache will retain automatic cache cluster snapshots before deleting them",
            "nullable": true,
            "type": "number"
        }],
        "apply_immediately": [
            {
                "default": false,
                "description": "Specifies whether any modifications are applied immediately, or during the next maintenance window",
                "nullable": true,
                "type": "bool"
            }
        ],
        "at_rest_encryption_enabled": [
            {
                "default": true,
                "description": "Whether to enable encryption at rest",
                "nullable": true,
                "type": "bool"
            }
        ],
        "multi_az_enabled": [
            {
                "default": false,
                "description": "Specifies whether Multi-AZ is enabled for the replication group",
                "nullable": true,
                "type": "bool"
            }
        ],
        "auth_token": [
            {
                "description": "The password used to access a password protected server",
                "nullable": true,
                "type": "string",
                "sensitive": true
            }
        ],
        "auth_token_update_strategy": [
            {
                "default": "ROTATE",
                "description": "Specifies the strategy to use to update the auth token",
                "nullable": true,
                "type": "string"
            }
        ],
        "auto_minor_version_upgrade": [
            {
                "default": true,
                "description": "Specifies whether minor engine upgrades are applied automatically to the replication group during the maintenance window",
                "nullable": true,
                "type": "bool"
            }
        ],
        "automatic_failover_enabled": [
            {
                "default": false,
                "description": "Specifies whether a read-only replica is automatically promoted to read/write primary if the existing primary fails",
                "nullable": true,
                "type": "bool"
            }
        ],
        "description": [
            {
                "description": "The description of the replication group",
                "nullable": false,
                "type": "string"
            }
        ],
        "engine": [
            {
                "description": "The name of the cache engine to be used for the clusters in this replication group",
                "nullable": true,
                "type": "string"
            }
        ],
        "engine_version": [
            {
                "description": "The version number of the cache engine to be used for the clusters in this replication group",
                "nullable": true,
                "type": "string"
            }
        ],
        "kms_key_id": [
            {
                "description": "The ARN of the KMS key to be used to encrypt the replication group",
                "nullable": true,
                "type": "string"
            }
        ],
        "maintenance_window": [
            {
                "description": "The window to perform maintenance in",
                "nullable": true,
                "type": "string"
            }
        ],
        "node_type": [
            {
                "description": "The compute and memory capacity of the nodes in the node group",
                "nullable": true,
                "type": "string"
            }
        ],
        "num_cache_clusters": [
            {
                "default": 1,
                "description": "The number of clusters this replication group will initially have",
                "nullable": true,
                "type": "number"
            }
        ],
        "parameter_group_name": [
            {
                "description": "The name of the parameter group to associate with this replication group",
                "nullable": true,
                "type": "string"
            }
        ],
        "port": [
            {
                "default": 6379,
                "description": "The port number on which each of the cache nodes will accept connections",
                "nullable": true,
                "type": "number"
            }
        ],
        "replication_group_id": [
            {
                "description": "The replication group identifier",
                "nullable": false,
                "type": "string"
            }
        ],
        "transit_encryption_enabled": [
            {
                "default": true,
                "description": "Whether to enable encryption in transit",
                "nullable": true,
                "type": "bool"
            }
        ],
        "tags": [
            {
                "type": "map(string)",
                "description": " Map of tags to assign to the resource. Adding tags to this resource will add or overwrite any existing tags on the clusters in the replication group and not to the group itself.",
                "nullable": true
            }
        ],
        "log_delivery_configuration": [
            {
                "description": "Configuration block for log delivery configuration.",
                "nullable": true,
                "type": "list(object({ destination: string, destination_type: string, log_format: string, log_type: string }))"
            }
        ]
    }
}
