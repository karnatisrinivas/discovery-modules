{
    "output": {
        "customer_router_config": {
            "description": "The configuration settings for the customer's router.",
            "value": "${data.aws_dx_router_configuration.this.customer_router_config}"
        },
        "id": {
            "description": "The unique identifier for the router configuration.",
            "value": "${data.aws_dx_router_configuration.this.id}"
        },
        "router": {
            "description": "A list of router details, including platform, type, software, vendor, and template names.",
            "value": "${data.aws_dx_router_configuration.this.router}"
        },
        "virtual_interface_name": {
            "description": "The name of the virtual interface associated with the router configuration.",
            "value": "${data.aws_dx_router_configuration.this.virtual_interface_name}"
        }
    }
}
