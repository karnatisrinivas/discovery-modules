version: 1.0

representation:
  side_panel:
    label: ECS Cluster Capacity Providers
  node:
    label:
      static: ECS Cluster Capacity Providers
      template: ECS Cluster Capacity Providers
    display_type: default

discovery:
  type: aws_ecs_cluster_capacity_providers
  dnd_supported: false
  references:
    cluster_name:
      from: aws_ecs_cluster
      output: name
