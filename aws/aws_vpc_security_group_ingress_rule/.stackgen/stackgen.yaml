version: 1.0

representation:
  side_panel:
    label: VPC Security Group Ingress Rule
  node:
    label:
      static: VPC Security Group Ingress Rule
      template: VPC Security Group Ingress Rule
    display_type: default

variables:
  ip_protocol:
    description: "The IP protocol name or number. Use -1 to specify all protocols."
    type: string
    options: 
      - tcp
      - udp
      - icmp
      - icmpv6
      - "-1"
    ui_control: select

discovery:
  type: aws_vpc_security_group_ingress_rule
  dnd_supported: false
  references:
    security_group_id:
      from: aws_security_group
      output: id
