{
    "variable": {
        "peer_vpc_id": [
            {
                "type": "string",
                "description": "The ID of the VPC to peer with.",
                "nullable": false
            }
        ],
        "peer_owner_id": [
            {
                "type": "string",
                "description": "The owner ID of the peer VPC.",
                "nullable": true
            }
        ],
        "peer_region": [
            {
                "type": "string",
                "description": "The region of the peer VPC.",
                "nullable": true
            }
        ],
        "vpc_id": [
            {
                "type": "string",
                "description": "The ID of the VPC to peer with.",
                "nullable": false
            }
        ],
        "auto_accept": [
            {
                "type": "bool",
                "description": "Whether to automatically accept the peering request.",
                "nullable": true
            }
        ],
        "accepter": [
            {
                "type": "list(object({allow_remote_vpc_dns_resolution: bool}))",
                "description": "The accepter configuration.",
                "nullable": true
            }
        ],
        "requester": [
            {
                "type": "list(object({allow_remote_vpc_dns_resolution: bool}))",
                "description": "The requester configuration.",
                "nullable": true
            }
        ],
        "tags": [
            {
                "type": "map(string)",
                "description": "The tags to apply to the VPC peering connection.",
                "nullable": true
            }
        ]
    }
}