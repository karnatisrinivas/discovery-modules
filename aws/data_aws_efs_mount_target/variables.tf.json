{
  "variable": {
    "access_point_id": [
      {
        "description": "The ID of the EFS access point to use for the mount target. This is optional and can be left empty if not needed.",
        "nullable": true,
        "type": "string"
      }
    ]
  }
}