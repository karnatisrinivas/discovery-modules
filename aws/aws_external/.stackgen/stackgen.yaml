version: 1.0

representation:
  side_panel:
    label: AWS External Resource
  node:
    label:
      static: AWS External Resource
      template: ${arn}
    display_type: default

discovery:
  type: aws_external
  dnd_supported: false
