{
  "variable": {
    "name_regex": [
      {
        "description": "A regular expression to filter IAM users by their names.",
        "nullable": true,
        "type": "string"
      }
    ],
    "path_prefix": [
      {
        "description": "The path prefix for filtering IAM users. It helps in organizing users under a specific path.",
        "nullable": true,
        "type": "string"
      }
    ]
  }
}