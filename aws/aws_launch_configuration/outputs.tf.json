{
    "output": {
        "arn": {
            "description": "The Amazon Resource Name (ARN) assigned to the launch configuration.",
            "value": "${aws_launch_configuration.this.arn}"
        },
        "id": {
            "description": "The unique ID of the launch configuration.",
            "value": "${aws_launch_configuration.this.id}"
        },
        "key_name": {
            "description": "The name of the key pair used to connect to the instances.",
            "value": "${aws_launch_configuration.this.key_name}"
        },
        "name": {
            "description": "The name of the launch configuration.",
            "value": "${aws_launch_configuration.this.name}"
        },
        "name_prefix": {
            "description": "The prefix used for the launch configuration name.",
            "value": "${aws_launch_configuration.this.name_prefix}"
        }
    }
}
