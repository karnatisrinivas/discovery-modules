version: 1.0

representation:
  side_panel:
    label: EMR Cluster
  node:
    label:
      static: EMR Cluster
      template: ${name}
    display_type: default

discovery:
  type: aws_emr_cluster
  dnd_supported: false
