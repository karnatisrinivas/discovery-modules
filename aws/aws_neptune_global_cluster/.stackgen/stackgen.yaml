version: 1.0

representation:
  side_panel:
    label: Neptune Global Cluster
  node:
    label:
      static: Neptune Global Cluster
      template: ${global_cluster_identifier}
    display_type: default

discovery:
  type: aws_neptune_global_cluster
  dnd_supported: false
  references:
    source_db_cluster_identifier:
      from: aws_neptune_cluster
      output: arn
