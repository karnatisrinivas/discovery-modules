version: 1.0

representation:
  side_panel:
    label: Ses Receipt Rule Set
  node:
    label:
      static: Ses Receipt Rule Set
      template: ${rule_set_name}
    display_type: default

discovery:
  type: aws_ses_receipt_rule_set
  dnd_supported: false
