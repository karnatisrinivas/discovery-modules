{
    "output": {
        "arn": {
            "description": "The value of the arn output",
            "value": "${aws_vpc.this.arn}"
        },
        "default_network_acl_id": {
            "description": "The value of the default_network_acl_id output",
            "value": "${aws_vpc.this.default_network_acl_id}"
        },
        "default_route_table_id": {
            "description": "The value of the default_route_table_id output",
            "value": "${aws_vpc.this.default_route_table_id}"
        },
        "id": {
            "description": "The value of the id output",
            "value": "${aws_vpc.this.id}"
        },
        "vpc_id": {
            "description": "The value of the vpc_id output",
            "value": "${aws_vpc.this.id}"
        }
    }
}
