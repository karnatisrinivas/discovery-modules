version: 1.0

representation:
  side_panel:
    label: ECS Cluster
  node:
    label:
      static: ECS Cluster
      template: ${name}
    display_type: default

discovery:
  type: aws_ecs_cluster
  dnd_supported: false
  data_source: data_aws_ecs_cluster
