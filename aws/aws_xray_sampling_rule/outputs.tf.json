{
    "output": {
        "arn": {
            "description": "Amazon Resource Name for integration with IAM policies and API calls",
            "value": "${aws_xray_sampling_rule.this.arn}"
        },
        "id": {
            "description": "Unique identifier for managing and referencing the sampling rule",
            "value": "${aws_xray_sampling_rule.this.id}"
        },
        "tags_all": {
            "description": "Consolidated tags for resource identification and organization",
            "value": "${aws_xray_sampling_rule.this.tags_all}"
        }
    }
}
