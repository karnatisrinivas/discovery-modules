{
    "output": {
        "arn": {
            "description": "The Amazon Resource Name (ARN) of the AWS Load Balancer trust store.",
            "value": "${data.aws_lb_trust_store.this.arn}"
        },
        "id": {
            "description": "The unique identifier of the AWS Load Balancer trust store.",
            "value": "${data.aws_lb_trust_store.this.id}"
        },
        "name": {
            "description": "The name of the AWS Load Balancer trust store.",
            "value": "${data.aws_lb_trust_store.this.name}"
        }
    }
}
