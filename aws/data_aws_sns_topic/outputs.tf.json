{
    "output": {
        "arn": {
            "description": "The Amazon Resource Name (ARN) uniquely identifying the SNS topic.",
            "value": "${data.aws_sns_topic.this.arn}"
        },
        "id": {
            "description": "The unique identifier assigned to the SNS topic.",
            "value": "${data.aws_sns_topic.this.id}"
        },
        "tags": {
            "description": "A map of tags assigned to the SNS topic.",
            "value": "${data.aws_sns_topic.this.tags}"
        }
    }
}
