{
    "output": {
        "id": {
            "description": "A unique identifier for the data source of AWS EC2 public IPv4 pools.",
            "value": "${data.aws_ec2_public_ipv4_pools.this.id}"
        },
        "pool_ids": {
            "description": "A list of IDs representing the available EC2 public IPv4 address pools.",
            "value": "${data.aws_ec2_public_ipv4_pools.this.pool_ids}"
        },
        "tags": {
            "description": "A map of tags associated with the EC2 public IPv4 pools for categorization and organization.",
            "value": "${data.aws_ec2_public_ipv4_pools.this.tags}"
        }
    }
}
