{
    "output": {
        "id": {
            "description": "Unique identifier for linking the notification configuration to other resources",
            "value": "${aws_autoscaling_notification.this.id}"
        }
    }
}
