{
    "output": {
        "admin_username": {
            "description": "The administrator username for the container registry.",
            "value": "${data.azurerm_container_registry.this.admin_username}"
        },
        "id": {
            "description": "The unique identifier of the container registry.",
            "value": "${data.azurerm_container_registry.this.id}"
        }
    }
}
