version: 1.0

representation:
  side_panel:
    label: "DNS - CNAME Record"
  node:
    label:
      static: "DNS - CNAME Record"
      template: ${name}
    display_type: default

discovery:
  type: azurerm_dns_cname_record
  dnd_supported: false
  references:
    zone_name:
      from: azurerm_dns_zone
      output: name
