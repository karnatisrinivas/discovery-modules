version: 1.0

representation:
  side_panel:
    label: "[DATA] EventHub Authorization Rule"
  node:
    label:
      static: "[DATA] EventHub Authorization Rule"
      template: ${name}
    display_type: default

discovery:
  type: data_azurerm_eventhub_authorization_rule
  dnd_supported: false
