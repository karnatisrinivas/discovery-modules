version: 1.0

representation:
  side_panel:
    label: "DNS - TXT Record"
  node:
    label:
      static: "DNS - TXT Record"
      template: ${name}
    display_type: default

discovery:
  type: azurerm_dns_txt_record
  dnd_supported: false
  references:
    zone_name:
      from: azurerm_dns_zone
      output: name
