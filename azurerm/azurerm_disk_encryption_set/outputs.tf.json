{
    "output": {
        "id": {
            "description": "Unique identifier for referencing the disk encryption set in other Azure resources",
            "value": "${azurerm_disk_encryption_set.this.id}"
        },
        "key_vault_key_url": {
            "description": "Key Vault key URL for managing encryption and securing data in disks",
            "value": "${azurerm_disk_encryption_set.this.key_vault_key_url}"
        },
        "location": {
            "description": "Azure region where the disk encryption set is deployed for resource allocation",
            "value": "${azurerm_disk_encryption_set.this.location}"
        },
        "resource_group_name": {
            "description": "Name of the resource group containing the disk encryption set for organizational purposes",
            "value": "${azurerm_disk_encryption_set.this.resource_group_name}"
        }
    }
}
