version: 1.0

representation:
  side_panel:
    label: "[Data] PostgreSQL DB Server"
  node:
    label:
      static: "[Data] PostgreSQL DB Server"
      template: ${name}
    display_type: default

discovery:
  type: data_azurerm_postgresql_server
  dnd_supported: false
